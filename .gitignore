node_modules/
out/
*.vsix
.headroom/
*.visual-check.*


# env files: real values are never committed (dotenvx-encrypted files are added explicitly)
.env
.env.*
!.env.example

# npm pack artifact (bun pm pack / npm pack)
*.tgz
